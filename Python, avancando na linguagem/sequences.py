# tuples -> immutable
tuple_type = ('name', 5)

# lists -> mutable
list_type = ['name', 6]

# set -> allows only unique elements
set_type = {'name'}  # doesn't have a index
set_type.add(7)  # doesn't use append, but add

# dictionary
dictionary_type = {'name': 'unknown', 'age': 8}
